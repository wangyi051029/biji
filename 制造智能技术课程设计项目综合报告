面向小样本工业工况预测：融合统计学习的深度学习智能计算优化方案
—— 制造智能技术课程设计项目综合报告
课程：制造智能技术　方法：vibe coding + Git 全程版本管理　报告日期：2026 年 9 月
摘要
本项目面向工业设备剩余使用寿命（RUL）预测这一典型小样本工业场景，采用 vibe coding 方法完成了一整套可交付、可运行、可追溯的智能计算系统。项目融合统计学习（数据清洗、噪声识别、特征筛选、交叉验证）、深度学习（双层 LSTM 时序网络）与智能计算优化（剪枝 + 量化）三大技术方向，在普通 PC（CPU）上构建了完整的 B/S 架构应用：前端 Web UI + FastAPI 后端 + SQLite 数据库，共 7 大功能页面，并通过 29 项自动化测试。数据采用 NASA C-MAPSS 公开数据集（4 个子集）。系统同时提供离线单文件测试站（模型权重内嵌 HTML、浏览器本地推理）与 GitHub Pages / Streamlit 云部署，任何电脑均可直接访问，实现了课程设计"数据 → 预处理 → 算法 → 系统 → 交付"的完整闭环。
一、项目背景与选题
工业设备（航空发动机、风机、机床等）的退化预测是制造智能的核心问题之一：通过传感器采集的工况数据，提前预测设备剩余寿命（RUL），支撑预测性维护决策。该类问题普遍存在小样本、高噪声、多工况等挑战，需要融合多种智能计算方法加以解决。
根据课程设计任务书，要求完成：选题说明与方案设计、数据来源与预处理、基于多种技术方向的完整演示系统（B/S 架构）、自动化测试、需求规格说明书、设计报告、答辩 PPT，以及 AI 使用（vibe coding）过程档案，并全程使用 Git 进行版本管理。本项目在此基础上进一步完成离线测试站与云部署，形成可在线访问的交付物。
二、总体方案与技术路线
系统按"数据层 → 算法层 → 服务层 → 表现层"四层架构组织，三大技术方向各司其职并形成业务闭环：
数据层：NASA C-MAPSS 原始数据、预处理结果、SQLite 数据库、模型权重；
算法层：统计学习预处理 / 深度学习 LSTM / 智能计算优化（剪枝 + 量化）；
服务层：FastAPI 后端，提供 REST API、业务编排与预测持久化；
表现层：前端 Web UI，7 大功能页面 + 手动测试。
三大技术方向在系统中的落点：
1.统计学习：数据清洗、IQR+Z-score 噪声识别、方差/相关/ANOVA 特征筛选、分段线性 RUL 标签、GroupKFold 交叉验证、标准化防泄漏。
2.深度学习：双层 LSTM（窗口 30，hidden 96）时序回归预测 RUL，Adam + 权重衰减 + Dropout + 早停 + 学习率调度。
3.智能计算优化：基于权重大小剪枝 + int8 模拟量化，在几乎不损失精度前提下显著压缩模型，适配普通 PC 部署。
三、数据资源与预处理
3.1 数据来源
采用 NASA 涡轮风扇发动机退化仿真数据集（C-MAPSS），含 FD001–FD004 四个子集（公开数据集，直链已验证有效）。各子集规模如下：
子集	训练单元	测试单元	传感器数	工况数	噪声率(%)
FD001	100	100	21	1	4
FD002	260	259	21	6	21
FD003	100	100	21	1	14
FD004	248	249	21	6	6
数据说明文档位于仓库 data/数据来源说明.md，来源链接与数据集元信息完整记录，满足课程设计对数据可追溯的要求。
3.2 预处理流程
预处理流水线（code/preprocess.py）按统计学习方法实现：
数据清洗 → 噪声识别（IQR + Z-score）→ 特征筛选（方差 + 相关 + ANOVA）→ 分段线性 RUL 标签 → 按单元划分 train/val/test → GroupKFold 5 折交叉验证 → 标准化（仅训练集拟合，防泄漏）。
预处理产物（data/processed/）包含各子集的 train/val/test 干净数据、特征筛选结果与标准化参数（scaler）。程序与说明文档齐全，满足任务书"数据预处理需更新 /data 目录与 README 同步"的要求。
四、系统架构与实现
层	技术	说明
前端	HTML / CSS / JavaScript	7 大功能页 + 手动测试页，总览含统计图表
后端	FastAPI + Uvicorn	REST API：数据集/模型/预测/实验/手动预测接口
数据	SQLite	datasets / models / predictions / experiments 四表
算法	PyTorch（CPU）/ scikit-learn	statistical / deep_learning / optimization 三模块
后端入口 backend/main.py，业务逻辑集中在 backend/services.py，数据库层 backend/database.py，算法模块位于 backend/algorithms/。代码结构清晰、职责分层，普通 PC（CPU）即可运行，无需 GPU。
五、算法模块设计
5.1 深度学习：双层 LSTM
LSTMRULModel:
  LSTM(input=15, hidden=96, layers=2, batch_first=True, dropout=0.2)
  Head: Linear(96→32) → ReLU → Dropout → Linear(32→1)
训练：Adam + 权重衰减 + SmoothL1(Huber) + ReduceLROnPlateau + 早停
滑动窗口构造时序样本（window=30, stride=1），每个样本为最近 30 个周期的特征序列，输出为该时刻的 RUL。预测时对输出做非负裁剪（clip ≥ 0）。
5.2 智能计算优化：剪枝 + 量化
剪枝：基于权重大小（magnitude）对 LSTM/全连接层剪去 30% 权重，形成稀疏模型。
量化：对剪枝后模型做 int8 模拟量化（quant_bits=8），显著降低存储占用。
对比：在相同测试窗口上评估基准与优化后模型的参数、体积、推理耗时与精度。
六、系统功能
完整版系统（backend/ + frontend/）共 7 大功能页面：
页面	功能
总览	数据集 / 模型 / 预测记录统计与趋势图表
数据管理	查看各子集预处理结果、执行预处理流水线
模型训练	选择子集/超参数训练 LSTM，实时展示 MAE/RMSE/参数量/耗时
智能优化	剪枝 + 量化，展示压缩前后对比
在线预测	选择子集/模型/单元，预测 RUL 并展示退化曲线
手动测试	直接输入传感器读数（每行一个周期），动态建表单并预测 RUL
对比分析	基准模型 vs 优化后模型指标对比
其中"手动测试"页为后期新增：后端提供 GET /api/model/info/ 与 POST /api/model/predict_manual，支持用户粘贴任意传感器序列进行即时预测，并自动按该子集的标准化参数处理、入库记录。
七、实验结果与指标
以 FD001 为演示子集，seed 脚本实测（可复现，仓库 backend/seed.py），关键指标如下：
指标	基准（LSTM）	优化后（剪枝30%+int8）	变化
参数量	121.03 K	85.23 K	-29.6%
模型体积	~484 KB	~85 KB	-82.4%
MAE	12.06	10.33	精度保持
RMSE	15.86	—	—
推理耗时	0.06 ms	0.13 ms	同量级
结论：剪枝 + 量化在几乎不损失精度的前提下，实现参数量减少约 30%、存储减少约 82%，验证了"融合方案降低算力开销、适配普通 PC"的设计目标。
八、测试与质量保障
自动化测试覆盖后端 API、服务层、数据库层与手动预测接口，共 29 项测试全部通过（backend/tests/ 四文件，一键 bash run_tests.sh）。
API 测试：数据集查询、模型查询、预测记录、在线预测、模型信息、手动预测接口（含参数校验：列数不匹配 400、空输入 400）。
服务层测试：预处理流水线、特征筛选、模型训练/加载、剪枝量化对比。
数据库测试：四表增查、预测记录写入。
手动预测接口：模型信息返回特征列表 / 窗口 / 示例读数；手动预测数值与 Python 基准一致。
离线单文件测试站另通过 JS 与 Python 逐位对比：同一输入序列下 JS 推理 RUL 与 Python 参考值误差小于 1e-5（float32 精度内一致）。
九、总结与展望
完成度：从选题、数据、算法、系统、测试、文档到在线交付，全部任务书要求已达成，Git 全程版本管理并推送 GitHub。
技术验证：三大技术方向融合形成完整闭环；LSTM 在 CPU 上实现毫秒级推理；剪枝量化压缩模型体积 82% 而精度保持。
可交付性：既有本地一键运行（zip + 脚本），又有离线单文件（双击即用）与在线网址（GitHub Pages / Streamlit），覆盖任意使用场景。
展望：可扩展多子集模型库、接入实时数据流、补充注意力机制等更先进时序模型，并将演示视频与部署步骤纳入最终答辩材料。

