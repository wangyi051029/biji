# 数据预处理说明

本目录中的预处理由 `code/preprocess.py` 完成，对应《方案设计》技术路线中**数据层**的工作：
> 利用假设检验、方差分析识别无效特征；统计方法识别噪声异常点；划分训练集、验证集、测试集，设置 k 折交叉验证。

## 1. 预处理流程

针对 C-MAPSS 四个子集（FD001~FD004），预处理依次执行以下步骤：

| 步骤 | 方法（统计学习） | 说明 |
|------|----------------|------|
| 1. 数据清洗 | 缺失值检测 + 常值/近常值检测 | 检查 NaN；剔除方差 < 1e-12 的无效传感器（如 FD001 中 s1/s5/s10/s16/s18/s19 为常值） |
| 2. 噪声识别 | IQR 准则 + Z-score 准则 | 逐传感器标记超出 [Q1-3IQR, Q3+3IQR] 或 \|Z\|>6 的噪声点（仅标记 `is_noise` 列，不删除，保留数据完整性） |
| 3. 特征筛选① | 方差分析 | 标准化后剔除低方差（<1e-3）特征 |
| 4. 特征筛选② | 相关性分析 | 剔除 |相关系数| > 0.95 的冗余特征，保留与 RUL 相关性更高的一个 |
| 5. 特征筛选③ | 单因素方差分析（ANOVA） | 按生命周期分 10 桶，f_oneway 检验传感器均值随退化阶段是否存在显著差异（p<0.05 保留） |
| 6. 标签构造 | 分段线性 RUL | RUL = min(max_cycle - cycle, 125)，模拟退化早期无明显信号 |
| 7. 数据集划分 | 按单元分组 | 每台最后 20% 周期划为测试集（模拟未观测到故障）；训练单元随机抽 15% 作验证集；其余为训练集 |
| 8. 交叉验证 | GroupKFold（k=5） | 按发动机单元分组，保证同一台不跨折，输出 5 折索引 |
| 9. 标准化 | StandardScaler | 仅在训练集上拟合均值/方差，再应用到验证/测试集，避免数据泄漏 |

## 2. 输出文件

```
data/processed/
├── preprocess_summary.json        # 全部子集处理统计汇总
├── FD001/                         # 各子集独立目录
│   ├── train_clean.csv            # 清洗+筛选+标准化后训练集
│   ├── val_clean.csv              # 验证集
│   ├── test_clean.csv             # 测试集
│   ├── feature_selection.json     # 特征筛选明细（剔除原因与保留列表）
│   ├── cv_folds.json              # 5 折交叉验证索引
│   └── scaler.json                # 标准化器参数（供后续建模复用）
├── FD002/
├── FD003/
└── FD004/
```

每份 `*_clean.csv` 字段：`unit`（单元号）、`cycle`（标准化周期）、筛选后的传感器特征（标准化）、`RUL`（标签）、`is_noise`（噪声标记）、`max_cycle`（单元最大周期）。

## 3. 预处理结果摘要

| 子集 | 原始传感器数 | 筛选后特征数 | 训练/验证/测试样本 | 噪声率 | 缺失值 |
|------|-----------|-----------|------------------|--------|--------|
| FD001 | 21 | 14 | 14024 / 2444 / 4163 | 5.93% | 0 |
| FD002 | 21 | 4  | 36651 / 6254 / 10854 | 15.90% | 0 |
| FD003 | 21 | 14 | 16825 / 2910 / 4985 | 15.31% | 0 |
| FD004 | 21 | 6  | 42004 / 6899 / 12346 | 15.42% | 0 |

> FD001/FD003 保留 14 个传感器，与 C-MAPSS 领域常用基准特征（14 传感器）一致；FD002/FD004 因多工况导致特征方差结构不同，保留特征更少。

## 4. 复现方式

```bash
# 在仓库根目录执行
python3 code/preprocess.py
```

依赖：Python 3.8+，`pandas`、`numpy`、`scipy`、`scikit-learn`。

## 5. 预处理结果的使用

预处理后的数据可直接用于下一阶段（阶段 3：深度学习基准模型构建）：
- 以筛选后的标准化特征作为输入，`RUL` 作为回归标签；
- 使用 `cv_folds.json` 中的 5 折索引进行交叉验证评估；
- 训练/验证/测试划分已按单元隔离，避免同一台发动机数据泄漏。
